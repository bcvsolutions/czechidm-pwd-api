# module properties - not editable through configuration service (idm.pub. prefix is not used)
# mapping pom.xml properties by default
# add custom properties if needed
#
# module version
module.pwd.build.version=@project.version@
# build number
module.pwd.build.buildNumber=@buildNumber@
module.pwd.build.buildTimestamp=@timestamp@
# module vendor
module.pwd.build.vendor=@project.organization.name@
module.pwd.build.vendorUrl=@project.organization.url@
module.pwd.build.vendorEmail=info@bcvsolutions.eu
# module description
module.pwd.build.name=@project.name@
module.pwd.build.description=@project.description@

# Module specific configuration
# idm.sec.pwd.configuration.propertyOne=value
